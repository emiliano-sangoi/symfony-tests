{% extends 'base.html.twig' %}

{% block body %}
    <h1>People list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Lastname</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for person in people %}
            <tr>
                <td><a href="{{ path('forms_ej1_show_person', { 'id': person.id }) }}">{{ person.id }}</a></td>
                <td>{{ person.name }}</td>
                <td>{{ person.lastName }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('forms_ej1_show_person', { 'id': person.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('forms_ej1_edit_person', { 'id': person.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('forms_ej1_new_person') }}">Create a new person</a>
        </li>
    </ul>
{% endblock %}
